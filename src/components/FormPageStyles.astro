<style is:global>
	.form-page {
		position: relative;
		isolation: isolate;
		width: 100%;
		margin: 0;
		padding: 56px 24px 72px;
		min-height: calc(100vh - 72px);
		min-height: calc(100svh - 72px);
		color: var(--color-text);
		background: var(--color-surface);
		font-family: 'Poppins', sans-serif;
	}

	.form-page::before {
		content: '';
		position: absolute;
		inset: 0;
		z-index: -1;
		background: inherit;
	}

	.form-page.form-theme-blue {
		background: var(--color-surface);
	}

	.form-page.form-theme-red {
		background: var(--color-accent);
		color: var(--color-text);
	}

	.form-page.form-theme-green {
		background: var(--color-strong);
	}

	.form-page.form-theme-green .form-hero h1,
	.form-page.form-theme-green .form-hero p {
		color: var(--color-on-surface-light);
	}

	.form-hero {
		text-align: center;
		padding: 56px 0 40px;
		max-width: 860px;
		margin: 0 auto;
	}

	.form-hero.image-hero {
		position: relative;
		isolation: isolate;
		max-width: 1120px;
		min-height: clamp(380px, 52vh, 560px);
		margin-bottom: 40px;
		padding: clamp(80px, 12vw, 140px) clamp(24px, 6vw, 72px);
		overflow: hidden;
		border-radius: 20px;
		background: var(--color-primary);
		display: flex;
		flex-direction: column;
		justify-content: flex-end;
	}

	.form-hero.image-hero::before,
	.form-hero.image-hero::after {
		content: '';
		position: absolute;
		inset: 0;
		pointer-events: none;
	}

	.form-hero.image-hero::before {
		z-index: 2;
		background: var(--hero-tint, rgba(84, 184, 237, 0.68));
		mix-blend-mode: multiply;
		opacity: 1;
		box-shadow:
			inset 0 0 0 1px rgba(255, 253, 247, 0.1),
			inset 18px 18px 42px rgba(17, 17, 17, 0.56),
			inset -18px -18px 42px rgba(17, 17, 17, 0.28);
	}

	.form-hero.image-hero::after {
		content: none;
	}

	.form-hero.image-hero > :not(img) {
		position: relative;
		z-index: 3;
	}

	.form-hero h1 {
		font-size: clamp(3rem, 7vw, 5.8rem);
		line-height: 1.06;
		margin: 0 0 24px;
		letter-spacing: -0.06em;
		color: var(--color-text);
	}

	.form-hero p {
		font-size: 1.18rem;
		max-width: 700px;
		margin: 0 auto;
		color: var(--color-body);
	}

	.form-hero.image-hero h1,
	.form-hero.image-hero p {
		color: var(--color-on-surface-light);
	}

	.form-section {
		display: grid;
		grid-template-columns: 1fr;
		gap: 44px;
		margin-top: 8px;
		max-width: 1120px;
		margin-left: auto;
		margin-right: auto;
		border-radius: 20px;
		padding: 32px;
		background:
			radial-gradient(circle at 18% 12%, rgba(255, 253, 247, 0.38), transparent 30%),
			radial-gradient(circle at 82% 20%, rgba(84, 184, 237, 0.1), transparent 32%),
			radial-gradient(circle at 72% 88%, rgba(220, 79, 65, 0.07), transparent 34%),
			linear-gradient(145deg, rgba(255, 253, 247, 0.34), rgba(255, 253, 247, 0.2) 48%, rgba(255, 253, 247, 0.3));
		border: 1px solid rgba(255, 253, 247, 0.62);
		backdrop-filter: blur(18px) saturate(1.2);
		-webkit-backdrop-filter: blur(18px) saturate(1.2);
		animation: none !important;
		transform: none !important;
		box-shadow:
			inset 0 1px 0 rgba(255, 253, 247, 0.42),
			inset 18px 18px 42px rgba(255, 253, 247, 0.12),
			0 24px 80px rgba(17, 17, 17, 0.14);
	}

	.hex-form h2 {
		font-size: 2rem;
		margin-bottom: 12px;
		color: var(--color-text);
		text-align: center;
	}

	.form-intro {
		max-width: 760px;
		margin: 0 auto 28px;
		color: var(--color-body);
		font-size: 1rem;
		line-height: 1.7;
		text-align: center;
	}

	.form-row {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 20px;
	}

	.form-group {
		margin-bottom: 20px;
	}

	.form-group label,
	.checkbox-group label {
		display: block;
		margin-bottom: 8px;
		font-weight: 600;
		color: var(--color-text);
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		width: 100%;
		padding: 12px;
		border: 1px solid rgba(255, 253, 247, 0.34);
		border-radius: 10px;
		font-family: 'Poppins', sans-serif;
		font-size: 1rem;
		background: rgba(255, 253, 247, 0.18);
		color: var(--color-text);
		backdrop-filter: blur(10px) saturate(1.1);
		-webkit-backdrop-filter: blur(10px) saturate(1.1);
		box-shadow:
			inset 4px 4px 10px rgba(0, 0, 0, 0.06),
			inset -4px -4px 10px rgba(255, 253, 247, 0.08);
		transition: border-color 0.2s ease, box-shadow 0.2s ease;
	}

	.form-group input::placeholder,
	.form-group textarea::placeholder {
		color: rgba(17, 17, 17, 0.42);
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		border-color: rgba(255, 253, 247, 0.76);
		outline: none;
		box-shadow:
			inset 4px 4px 10px rgba(0, 0, 0, 0.08),
			inset -4px -4px 10px rgba(255, 253, 247, 0.1),
			0 0 0 3px rgba(255, 253, 247, 0.18);
	}

	.form-group select option {
		background: var(--color-surface-muted);
		color: var(--color-text);
	}

	.form-group select {
		appearance: auto;
	}

	.form-group textarea {
		resize: vertical;
		min-height: 132px;
	}

	.form-group textarea.compact-textarea {
		min-height: 84px;
	}

	.checkbox-group {
		margin: 0 0 20px;
	}

	.checkbox-label {
		display: grid;
		grid-template-columns: 22px 1fr;
		gap: 12px;
		align-items: start;
		font-weight: 400;
		line-height: 1.6;
	}

	.checkbox-label input {
		width: 18px;
		height: 18px;
		margin-top: 4px;
		accent-color: var(--color-strong);
	}

	.turnstile-wrap {
		margin: 0 0 20px;
		min-height: 65px;
	}

	.hex-form .button {
		cursor: pointer;
	}

	.form-submit {
		display: flex;
		justify-content: center;
	}

	.form-next-actions {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		max-width: 1120px;
		margin: 32px auto 0;
		min-height: 320px;
		padding: 72px 32px;
		border-radius: 20px;
		background: var(--color-strong);
		border: 0;
		text-align: center;
		box-shadow: var(--shadow-inset-box);
		animation: none !important;
		transform: none !important;
	}

	.form-next-actions h2,
	.form-next-actions p {
		color: var(--color-on-surface-light);
		max-width: 760px;
		margin-left: auto;
		margin-right: auto;
	}

	.form-next-actions h2 {
		margin: 0 0 10px;
		font-size: clamp(1.65rem, 3vw, 2.4rem);
		line-height: 1.08;
	}

	.form-next-actions p {
		max-width: 680px;
		margin: 0 auto 22px;
		line-height: 1.7;
	}

	.form-next-buttons {
		display: flex;
		flex-wrap: wrap;
		gap: 14px;
		justify-content: center;
	}

	.form-next-actions .button {
		background: var(--color-on-surface-light);
		color: var(--color-text);
	}

	.form-next-actions .button:hover {
		background: var(--color-primary);
		color: var(--color-on-surface-light);
	}

	@media (max-width: 800px) {
		.form-page {
			padding: 32px 18px 48px;
		}

		.form-hero {
			padding: 40px 0 28px;
		}

		.form-row {
			grid-template-columns: 1fr;
			gap: 0;
		}

		.form-section {
			padding: 24px 18px;
		}
	}
</style>
